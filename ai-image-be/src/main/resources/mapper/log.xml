<?xml version="1.0" encoding= "UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.aiimage.mapper.LogMapper">
	<insert id="login">
		INSERT INTO t_login_history (actor, ip_addr, dtt) VALUE (#{actor}, #{ipAddr}, now());
	</insert>

	<insert id="access">
		INSERT INTO t_access_history (actor, menu, action, size, ip_addr, dtt)
			VALUE (#{ actor }, #{ menu }, #{ action }, #{ size }, #{ ipAddr }, now())
	</insert>
</mapper>
